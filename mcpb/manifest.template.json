{
  "$schema": "https://raw.githubusercontent.com/anthropics/mcpb/main/schemas/mcpb-manifest-v0.4.schema.json",
  "manifest_version": "0.4",
  "name": "korea-scholarship-mcp",
  "display_name": "Korea Scholarship (KCI + OAK) (bibliograph)",
  "version": "0.5.1",
  "description": "KCI (Korea Citation Index) and Open Access Korea: Korean articles, references, journal metrics and OAI-PMH harvests, with typed envelopes and query receipts.",
  "long_description": "One of the six bibliograph MCP servers (https://github.com/ckgerteis/bibliograph-mcp). Every search answers with one typed JSON envelope: the term actually sent and its script, how the source matched it, how broad the set is, typed diagnostics, a receipt hash, and the provider's attribution. Receipts can be deposited to an append-only, hash-chained ledger so a search behind a footnote can be named and re-run. Requires a Python 3.10+ interpreter on this machine; the bundle carries every library it needs.",
  "author": {
    "name": "Christopher Gerteis",
    "email": "christopher.gerteis@gmail.com",
    "url": "https://www.christophergerteis.net"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ckgerteis/korea-scholarship-mcp.git"
  },
  "homepage": "https://github.com/ckgerteis/korea-scholarship-mcp",
  "documentation": "https://github.com/ckgerteis/korea-scholarship-mcp#readme",
  "support": "https://github.com/ckgerteis/korea-scholarship-mcp/issues",
  "license": "MIT",
  "keywords": [
    "bibliographic-search",
    "digital-humanities",
    "kci",
    "korean-studies",
    "mcp",
    "model-context-protocol",
    "oai-pmh",
    "oak",
    "query-receipts",
    "scholarly-metadata"
  ],
  "server": {
    "type": "python",
    "entry_point": "server/main.py",
    "mcp_config": {
      "command": "__PYTHON__",
      "args": [
        "${__dirname}/server/main.py"
      ],
      "env": {
        "KCI_API_KEY": "${user_config.kci_api_key}",
        "MCP_RECEIPT_DIR": "${user_config.receipts_dir}",
        "MCP_RECEIPT_SESSION": "${user_config.receipt_session}"
      }
    }
  },
  "user_config": {
    "kci_api_key": {
      "type": "string",
      "title": "KCI API key",
      "description": "Optional. Without it the four KCI REST tools do not run; OAK harvest, OAK record and the status tool still do.",
      "sensitive": true,
      "required": false
    },
    "receipts_dir": {
      "type": "directory",
      "title": "Receipts folder",
      "description": "Where this server deposits its append-only, hash-chained query receipts (one <server>.jsonl per server). Leave empty to run without receipts; the envelope will then say RECEIPT_NOT_DEPOSITED.",
      "required": false
    },
    "receipt_session": {
      "type": "string",
      "title": "Receipt session slug",
      "description": "A project or article slug stamped on every receipt, e.g. risk-of-history-2026. Optional.",
      "required": false
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "__PLATFORM__"
    ],
    "runtimes": {
      "python": ">=3.10"
    }
  }
}
